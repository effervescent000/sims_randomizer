import React from "react";

import GeneratePanel from "./generate-panel";

const Sims3Wrapper = (props) => {
    return (
        <div className="generate-panel">
            <GeneratePanel />
        </div>
    );
};

export default Sims3Wrapper;
