import React from "react";

import GeneratePanel from "./generate-panel";

const Sims4Wrapper = (props) => {
    return (
        <div>
            <GeneratePanel />
        </div>
    );
};

export default Sims4Wrapper;
